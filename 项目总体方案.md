

---

# 一、项目总体方案

## 1.1 目标

做一个支持 PC / 手机的 C++ 刷题网站：

- 支持历年真题按年份练习
- 支持顺序练习、随机练习、错题本、收藏夹
- 支持选择题自动判分
- 支持填空题、程序填空题、程序阅读题自动或半自动判分
- 支持编程题显示 AI 参考答案并自评
- 无后端，纯静态文件，可部署到 GitHub Pages
- 题库使用 JSON 文件，支持后续新增题目
- 支持本机进度保存、导出、导入
- 支持 PWA，可加到手机桌面

---

## 1.2 技术选型

在不使用后端的前提下，最优方案是：

| 模块          | 方案                                             | 原因                            |
| ------------- | ------------------------------------------------ | ------------------------------- |
| 前端页面      | 原生 HTML/CSS/JS                                 | 无需构建，GitHub Pages 直接部署 |
| 题库数据      | JSON 文件                                        | 易于新增、拆分、版本管理        |
| 学习进度      | LocalStorage                                     | 无后端时最稳定                  |
| 多端同步      | 导出 / 导入 JSON                                 | 无需后端                        |
| 部署          | GitHub Pages                                     | 静态资源即可                    |
| 离线 / 加桌面 | PWA manifest + Service Worker                    | 提升移动端体验                  |
| AI 参考答案   | 预先生成到 JSON 的 `analysis` / `reference` 字段 | 不依赖运行时 AI 接口            |

---

# 二、真题解析规划

根据你提供的五套真题，我先梳理出题库生产结构。

| 试卷       | 选择题 | 填空题 | 程序填空 | 程序分析 | 程序设计 | 估计题量 |
| ---------- | -----: | -----: | -------: | -------: | -------: | -------: |
| 2024年4月  |     20 |     15 |        5 |        5 |        2 |       47 |
| 2024年10月 |     20 |     10 |        5 |        5 |        2 |       42 |
| 2025年4月  |     20 |     10 |        5 |        5 |        2 |       42 |
| 2025年10月 |     20 |      0 |        5 |        5 |        2 |       32 |
| 2026年4月  |     10 |      5 |        2 |        4 |        2 |       23 |
| 合计       |     90 |     40 |       22 |       24 |       10 |      186 |

后续题库会按这些题型统一建模。

---

# 三、项目目录结构

建议创建仓库：

```text
cpp-quiz/
├── index.html
├── manifest.webmanifest
├── sw.js
├── assets/
│   ├── app.js
│   └── style.css
├── data/
│   ├── manifest.json
│   └── questions/
│       ├── sample.json
│       ├── 2024-04.json
│       ├── 2024-10.json
│       ├── 2025-04.json
│       ├── 2025-10.json
│       └── 2026-04.json
├── tools/
│   ├── build-manifest.mjs
│   └── validate.mjs
└── raw/
    ├── 202404.txt
    ├── 202410.txt
    ├── 202504.txt
    ├── 202510.txt
    └── 202604.txt
```

说明：

- `data/questions/` 存放题库资源文件。
- `data/manifest.json` 告诉网页要加载哪些 JSON。
- 新增题目时，可以新增一个 JSON 文件，也可以往已有 JSON 中追加题目。
- `tools/build-manifest.mjs` 用于自动生成 `data/manifest.json`。
- `tools/validate.mjs` 用于校验题库 JSON 是否合法。

---

# 四、题库资源文件设计

## 4.1 题库文件顶层结构

每个题库 JSON 文件建议如下：

```json
{
  "version": 1,
  "questions": [
    {
      "id": "202604-choice-1",
      "paper": "2026-04",
      "section": "单项选择题",
      "type": "single_choice",
      "stem": "下列C++标识符中，合法的是",
      "options": {
        "A": "123abc",
        "B": "abc_123",
        "C": "int",
        "D": "abc-123"
      },
      "answer": "B",
      "analysis": "C++标识符不能以数字开头，不能是关键字，不能包含减号。",
      "tags": ["标识符", "基础"],
      "difficulty": 1,
      "source": "2026年4月真题"
    }
  ]
}
```

---

## 4.2 题目通用字段

| 字段                            | 说明                                     |
| ------------------------------- | ---------------------------------------- |
| `id`                            | 全局唯一，建议格式：`年份月份-题型-题号` |
| `paper`                         | 试卷，例如 `2026-04`                     |
| `section`                       | 大题名称，例如 `单项选择题`              |
| `type`                          | 题型                                     |
| `stem`                          | 题干                                     |
| `options`                       | 选择题选项                               |
| `answer`                        | 标准答案                                 |
| `accepted`                      | 可接受的等价答案                         |
| `code` 或 `codeLines`           | 程序代码                                 |
| `blanks`                        | 程序填空的空                             |
| `reference` 或 `referenceLines` | 编程题参考代码                           |
| `analysis`                      | AI 参考解析                              |
| `tags`                          | 标签                                     |
| `difficulty`                    | 难度，1-5                                |
| `source`                        | 来源                                     |

---

## 4.3 支持的题型

### 1. 单项选择题

```json
{
  "id": "202604-choice-1",
  "paper": "2026-04",
  "section": "单项选择题",
  "type": "single_choice",
  "stem": "下列C++标识符中，合法的是",
  "options": {
    "A": "123abc",
    "B": "abc_123",
    "C": "int",
    "D": "abc-123"
  },
  "answer": "B",
  "analysis": "标识符只能由字母、数字、下划线组成，且不能以数字开头，不能是关键字。"
}
```

---

### 2. 填空题

```json
{
  "id": "202604-fill-15",
  "paper": "2026-04",
  "section": "填空题",
  "type": "fill_blank",
  "stem": "C++中，标准输入流对象是________。",
  "answer": ["cin"],
  "analysis": "C++标准输入流对象是cin。"
}
```

多个空：

```json
{
  "id": "xxx-fill-1",
  "type": "fill_blank",
  "stem": "第一空________，第二空________。",
  "answer": ["答案1", "答案2"],
  "analysis": "按顺序填写两个空。"
}
```

---

### 3. 程序填空题

```json
{
  "id": "202604-code-fill-20",
  "paper": "2026-04",
  "section": "程序填空题",
  "type": "code_fill",
  "stem": "以下程序实现求两个整数的最大值，请补全代码。",
  "codeLines": [
    "#include<iostream>",
    "using namespace std;",
    "int max(____①____){ return a>b? a: b; }",
    "int main(){",
    "    int x,y;",
    "    cin>> x>> y;",
    "    cout<<____②____<< endl;",
    "    return 0;",
    "}"
  ],
  "blanks": [
    {
      "id": "①",
      "answers": ["int a, int b", "int a,int b"]
    },
    {
      "id": "②",
      "answers": ["max(x, y)", "max(x,y)"]
    }
  ],
  "analysis": "函数形参需要声明两个整型变量；主函数中调用max函数。"
}
```

---

### 4. 程序阅读 / 输出题

```json
{
  "id": "202604-code-output-16",
  "paper": "2026-04",
  "section": "程序阅读题",
  "type": "code_output",
  "stem": "写出下列程序的运行结果。",
  "codeLines": [
    "#include<iostream>",
    "using namespace std;",
    "int main(){",
    "    int a=3, b=5;",
    "    int *p=&a, *q=&b;",
    "    *p=*p+*q;",
    "    cout<< a<<\",\"<< b<< endl;",
    "    return 0;",
    "}"
  ],
  "answer": ["8,5"],
  "analysis": "*p就是a，*p+*q等于8，所以a变为8，b仍为5。"
}
```

---

### 5. 程序设计题

```json
{
  "id": "202604-coding-22",
  "paper": "2026-04",
  "section": "程序设计题",
  "type": "coding",
  "stem": "编写一个学生类Student，包含学号、姓名、成绩，并能输出信息。",
  "referenceLines": [
    "#include <iostream>",
    "#include <string>",
    "using namespace std;",
    "class Student {",
    "private:",
    "    string id;",
    "    string name;",
    "    double score;",
    "public:",
    "    Student(string i, string n, double s) : id(i), name(n), score(s) {}",
    "    void showInfo() {",
    "        cout << id << ' ' << name << ' ' << score << endl;",
    "    }",
    "};",
    "int main() {",
    "    Student s(\"2026001\", \"ZhangSan\", 90.5);",
    "    s.showInfo();",
    "    return 0;",
    "}"
  ],
  "analysis": "定义私有数据成员，公有构造函数和成员函数，在主函数中创建对象并调用成员函数。"
}
```

---

# 五、H5 刷题网站实现

下面是一套可直接使用的静态 H5 刷题网站代码。

---

## 5.1 index.html

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>C++真题刷题</title>
  <link rel="stylesheet" href="assets/style.css">
  <link rel="manifest" href="manifest.webmanifest">
  <meta name="theme-color" content="#2563eb">
</head>
<body>
  <header class="topbar">
    <div class="brand">C++真题刷题</div>
    <div class="topbar-right">
      <span class="muted">本地进度</span>
    </div>
  </header>

  <main id="view" class="view" tabindex="-1"></main>

  <nav class="tabbar">
    <a href="#/" data-tab="home">首页</a>
    <a href="#/list?type=all" data-tab="list">题库</a>
    <a href="#/list?type=wrong" data-tab="wrong">错题</a>
    <a href="#/list?type=fav" data-tab="fav">收藏</a>
    <a href="#/settings" data-tab="settings">设置</a>
  </nav>

  <script src="assets/app.js"></script>
  <script>
    if ('serviceWorker' in navigator && location.protocol.startsWith('http')) {
      navigator.serviceWorker.register('sw.js').catch(() => {});
    }
  </script>
</body>
</html>
```

---

## 5.2 assets/style.css

```css
:root {
  --bg: #f3f6fb;
  --card: #ffffff;
  --text: #111827;
  --muted: #6b7280;
  --line: #e5e7eb;
  --primary: #2563eb;
  --primary-text: #ffffff;
  --success: #16a34a;
  --danger: #dc2626;
  --warning: #f59e0b;
  --radius: 16px;
  --shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
  background: var(--bg);
  color: var(--text);
}

.topbar {
  position: sticky;
  top: 0;
  z-index: 20;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 16px;
  background: rgba(255,255,255,.9);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--line);
}

.brand {
  font-weight: 800;
}

.view {
  max-width: 980px;
  margin: 0 auto;
  padding: 16px 16px 96px;
}

.card {
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 16px;
  margin-bottom: 16px;
}

.cards {
  display: grid;
  gap: 12px;
}

h1 {
  margin: 0 0 8px;
  font-size: 24px;
}

h2 {
  margin: 0 0 8px;
  font-size: 20px;
}

h3 {
  margin: 12px 0 8px;
  font-size: 16px;
}

p {
  line-height: 1.7;
}

.muted {
  color: var(--muted);
  font-size: 14px;
}

.stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
  gap: 10px;
  margin: 12px 0;
}

.stat {
  padding: 12px;
  border-radius: 12px;
  background: #f8fafc;
  border: 1px solid var(--line);
}

.stat b {
  display: block;
  font-size: 24px;
  margin-top: 4px;
}

.btn-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 12px;
}

.btn {
  appearance: none;
  border: 1px solid var(--line);
  background: #fff;
  color: var(--text);
  border-radius: 12px;
  padding: 10px 14px;
  font-size: 14px;
  cursor: pointer;
}

.btn.primary {
  background: var(--primary);
  border-color: var(--primary);
  color: var(--primary-text);
}

.btn.danger {
  background: var(--danger);
  border-color: var(--danger);
  color: #fff;
}

.btn:disabled {
  opacity: .5;
  cursor: not-allowed;
}

.option {
  display: block;
  width: 100%;
  text-align: left;
  margin: 8px 0;
  padding: 12px;
  border-radius: 12px;
  border: 1px solid var(--line);
  background: #fff;
  cursor: pointer;
}

.option.selected {
  border-color: var(--primary);
  background: #eff6ff;
}

.option.correct {
  border-color: var(--success);
  background: #f0fdf4;
}

.option.wrong {
  border-color: var(--danger);
  background: #fef2f2;
}

.code {
  white-space: pre-wrap;
  background: #0f172a;
  color: #e5e7eb;
  padding: 14px;
  border-radius: 12px;
  overflow: auto;
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 13px;
  line-height: 1.7;
}

input[type="text"],
textarea {
  width: 100%;
  padding: 12px;
  border-radius: 12px;
  border: 1px solid var(--line);
  background: #fff;
  font: inherit;
  margin: 8px 0;
}

textarea {
  min-height: 140px;
  resize: vertical;
}

.blank-item,
.fill-item {
  margin: 10px 0;
  padding: 10px;
  border: 1px dashed var(--line);
  border-radius: 12px;
}

.tabbar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 30;
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  background: rgba(255,255,255,.96);
  border-top: 1px solid var(--line);
  padding-bottom: env(safe-area-inset-bottom);
}

.tabbar a {
  padding: 12px 0;
  text-align: center;
  text-decoration: none;
  color: var(--muted);
  font-size: 14px;
}

.tabbar a.active {
  color: var(--primary);
  font-weight: 700;
}

.badge {
  display: inline-block;
  padding: 4px 8px;
  border-radius: 999px;
  background: #eef2ff;
  color: #3730a3;
  font-size: 12px;
  margin-right: 6px;
}

.good {
  outline: 2px solid rgba(22,163,74,.18);
}

.bad {
  outline: 2px solid rgba(220,38,38,.18);
}

.analysis {
  margin-top: 14px;
  padding: 12px;
  border-radius: 12px;
  background: #fffbeb;
  border: 1px solid #fde68a;
}

.file-btn {
  display: inline-block;
  margin-top: 10px;
  padding: 10px 14px;
  border: 1px solid var(--line);
  border-radius: 12px;
  background: #fff;
  cursor: pointer;
}

.file-btn input {
  display: none;
}

@media (min-width: 768px) {
  .cards {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}
```

---

## 5.3 assets/app.js

```js
(function () {
  const view = document.getElementById('view');
  const STORAGE_KEY = 'cpp_quiz_v1';

  const defaultDb = {
    progress: {},
    wrong: [],
    fav: [],
    customQuestions: [],
    settings: {}
  };

  let db = loadDb();
  let bank = { questions: [] };
  let quiz = loadQuiz();

  function loadDb() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      return Object.assign({}, defaultDb, raw ? JSON.parse(raw) : {});
    } catch (e) {
      return Object.assign({}, defaultDb);
    }
  }

  function saveDb() {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(db));
  }

  function loadQuiz() {
    try {
      return JSON.parse(sessionStorage.getItem('quiz') || 'null');
    } catch (e) {
      return null;
    }
  }

  function saveQuiz() {
    sessionStorage.setItem('quiz', JSON.stringify(quiz));
  }

  function esc(s) {
    return String(s == null ? '' : s).replace(/[&<>"']/g, function (m) {
      return {
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#39;'
      }[m];
    });
  }

  function nl2br(s) {
    return esc(s).replace(/\n/g, '<br>');
  }

  function truncate(s, n) {
    s = String(s || '');
    return s.length > n ? s.slice(0, n) + '...' : s;
  }

  function getCode(q) {
    return q.code || (q.codeLines || []).join('\n') || '';
  }

  function getRef(q) {
    return q.reference || (q.referenceLines || []).join('\n') || '';
  }

  function norm(s) {
    return String(s == null ? '' : s)
      .trim()
      .replace(/\u00A0/g, ' ')
      .replace(/\s+/g, ' ');
  }

  function normLoose(s) {
    return norm(s).replace(/\s+/g, '').toLowerCase();
  }

  function normOutput(s) {
    return String(s == null ? '' : s)
      .replace(/\r/g, '')
      .split('\n')
      .map(function (x) { return x.trim(); })
      .filter(Boolean)
      .join('\n');
  }

  function matchOne(input, answers) {
    return (answers || []).some(function (a) {
      return norm(input) === norm(a) || normLoose(input) === normLoose(a);
    });
  }

  function matchOutput(input, answers) {
    const a = normOutput(input);
    return (answers || []).some(function (ans) {
      const b = normOutput(ans);
      return a === b || a.replace(/\s+/g, '') === b.replace(/\s+/g, '');
    });
  }

  function shuffle(arr) {
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      const t = arr[i];
      arr[i] = arr[j];
      arr[j] = t;
    }
    return arr;
  }

  function allQuestions() {
    return bank.questions.concat(db.customQuestions || []);
  }

  function qById(id) {
    return allQuestions().find(function (q) { return q.id === id; });
  }

  function getProgress(id) {
    return db.progress[id] || {};
  }

  function setProgress(id, patch) {
    db.progress[id] = Object.assign({}, getProgress(id), patch);
    saveDb();
  }

  function isFav(id) {
    return db.fav.indexOf(id) !== -1;
  }

  function isWrong(id) {
    return db.wrong.indexOf(id) !== -1;
  }

  function addWrong(id) {
    if (!isWrong(id)) {
      db.wrong.push(id);
      saveDb();
    }
  }

  function removeWrong(id) {
    db.wrong = db.wrong.filter(function (x) { return x !== id; });
    saveDb();
  }

  function toggleFav(id) {
    if (isFav(id)) {
      db.fav = db.fav.filter(function (x) { return x !== id; });
    } else {
      db.fav.push(id);
    }
    saveDb();
    render();
  }

  async function loadData() {
    try {
      let files = [];

      try {
        const manifest = await fetch('data/manifest.json', { cache: 'no-cache' })
          .then(function (r) { return r.ok ? r.json() : null; });
        files = (manifest && manifest.files) || [];
      } catch (e) {
        files = [];
      }

      if (!files.length) {
        files = ['data/bank.json'];
      }

      const questions = [];

      for (const file of files) {
        try {
          const data = await fetch(file, { cache: 'no-cache' })
            .then(function (r) { return r.json(); });

          if (Array.isArray(data)) {
            questions.push.apply(questions, data);
          } else if (data && Array.isArray(data.questions)) {
            questions.push.apply(questions, data.questions);
          }
        } catch (err) {
          console.warn('题库加载失败:', file, err);
        }
      }

      bank.questions = questions;
    } catch (e) {
      bank.questions = [];
    }
  }

  function parseHash() {
    const raw = location.hash.slice(1) || '/';
    const parts = raw.split('?');
    const path = parts[0] || '/';
    const params = new URLSearchParams(parts[1] || '');
    return { path: path, params: params };
  }

  function updateNav(path, params) {
    const links = document.querySelectorAll('.tabbar a');
    links.forEach(function (a) {
      const tab = a.getAttribute('data-tab');
      let active = false;

      if (tab === 'home' && path === '/') active = true;
      if (tab === 'list' && path === '/list' && params.get('type') === 'all') active = true;
      if (tab === 'wrong' && path === '/list' && params.get('type') === 'wrong') active = true;
      if (tab === 'fav' && path === '/list' && params.get('type') === 'fav') active = true;
      if (tab === 'settings' && path === '/settings') active = true;

      a.classList.toggle('active', active);
    });
  }

  function render() {
    const route = parseHash();
    updateNav(route.path, route.params);

    if (route.path === '/') {
      renderHome();
    } else if (route.path === '/quiz') {
      renderQuiz();
    } else if (route.path === '/report') {
      renderReport();
    } else if (route.path === '/list') {
      renderList(route.params);
    } else if (route.path === '/settings') {
      renderSettings();
    } else {
      view.innerHTML = '<div class="card">页面不存在</div>';
    }
  }

  function stats() {
    const progressValues = Object.values(db.progress);
    const done = progressValues.filter(function (p) { return p.judged; }).length;
    const right = progressValues.filter(function (p) { return p.judged && p.correct; }).length;
    const acc = done ? Math.round(right / done * 100) : 0;

    return {
      total: allQuestions().length,
      done: done,
      right: right,
      acc: acc,
      wrong: db.wrong.length,
      fav: db.fav.length
    };
  }

  function renderHome() {
    const s = stats();
    const papers = {};

    allQuestions().forEach(function (q) {
      const key = q.paper || '未分组';
      if (!papers[key]) papers[key] = [];
      papers[key].push(q);
    });

    let html = `
      <section class="card">
        <h1>自考C++真题刷题</h1>
        <div class="muted">无后端静态版：进度保存在本机浏览器，可导出/导入。</div>
        <div class="stats">
          <div class="stat">总题数<b>${s.total}</b></div>
          <div class="stat">已练习<b>${s.done}</b></div>
          <div class="stat">正确率<b>${s.acc}%</b></div>
          <div class="stat">错题<b>${s.wrong}</b></div>
          <div class="stat">收藏<b>${s.fav}</b></div>
        </div>
        <div class="btn-row">
          <button class="btn primary" data-action="start" data-mode="order">全部顺序</button>
          <button class="btn" data-action="start" data-mode="random">全部随机</button>
          <button class="btn danger" data-action="start" data-source="wrong">错题练习</button>
          <button class="btn" data-action="start" data-source="fav">收藏练习</button>
        </div>
      </section>
      <section class="cards">
    `;

    Object.keys(papers).sort().forEach(function (paper) {
      const items = papers[paper];
      const done = items.filter(function (q) { return getProgress(q.id).judged; }).length;

      html += `
        <div class="card">
          <h2>${esc(paper)}</h2>
          <div class="muted">共 ${items.length} 题 / 已练 ${done} 题</div>
          <div class="btn-row">
            <button class="btn primary" data-action="start" data-paper="${esc(paper)}" data-mode="order">顺序练习</button>
            <button class="btn" data-action="start" data-paper="${esc(paper)}" data-mode="random">随机练习</button>
          </div>
        </div>
      `;
    });

    html += '</section>';
    view.innerHTML = html;
  }

  function renderList(params) {
    const type = params.get('type') || 'all';
    const paper = params.get('paper') || '';
    let qs = allQuestions();

    if (type === 'wrong') {
      qs = qs.filter(function (q) { return isWrong(q.id); });
    }

    if (type === 'fav') {
      qs = qs.filter(function (q) { return isFav(q.id); });
    }

    if (paper) {
      qs = qs.filter(function (q) { return q.paper === paper; });
    }

    const title = type === 'wrong' ? '错题本' : type === 'fav' ? '收藏夹' : '题库列表';
    const shown = qs.slice(0, 300);

    let html = `
      <section class="card">
        <h1>${title}</h1>
        <div class="muted">共 ${qs.length} 题${qs.length > 300 ? '，仅显示前300题' : ''}</div>
        <div class="btn-row">
          <button class="btn primary" data-action="start" data-source="${esc(type)}" data-mode="order">开始本列表</button>
        </div>
      </section>
      <section class="cards">
    `;

    shown.forEach(function (q, idx) {
      html += `
        <div class="card">
          <div class="q-title">${idx + 1}. ${esc(truncate(q.stem || '', 70))}</div>
          <div class="muted" style="margin:8px 0;">
            <span class="badge">${esc(q.paper || '未分组')}</span>
            <span class="badge">${esc(q.section || q.type || '')}</span>
          </div>
          <div class="btn-row">
            <button class="btn primary" data-action="start-single" data-id="${esc(q.id)}">作答</button>
            <button class="btn" data-action="toggle-fav" data-id="${esc(q.id)}">${isFav(q.id) ? '取消收藏' : '收藏'}</button>
            ${type === 'wrong' ? `<button class="btn danger" data-action="remove-wrong" data-id="${esc(q.id)}">移出错题</button>` : ''}
          </div>
        </div>
      `;
    });

    html += '</section>';
    view.innerHTML = html;
  }

  function startQuiz(options) {
    let qs = allQuestions();

    if (options.paper) {
      qs = qs.filter(function (q) { return q.paper === options.paper; });
    }

    if (options.source === 'wrong') {
      qs = qs.filter(function (q) { return isWrong(q.id); });
    }

    if (options.source === 'fav') {
      qs = qs.filter(function (q) { return isFav(q.id); });
    }

    if (options.mode === 'random') {
      shuffle(qs);
    }

    if (!qs.length) {
      alert('当前没有可练习的题目');
      return;
    }

    quiz = {
      qids: qs.map(function (q) { return q.id; }),
      index: 0
    };

    saveQuiz();
    location.hash = '#/quiz';
  }

  function startSingle(id) {
    quiz = {
      qids: [id],
      index: 0
    };
    saveQuiz();
    location.hash = '#/quiz';
  }

  function questionMetaHtml(q) {
    return `
      <div class="q-meta" style="margin-bottom:10px;">
        <span class="badge">${esc(q.paper || '未分组')}</span>
        <span class="badge">${esc(q.section || '')}</span>
        <span class="badge">${esc(q.type || '')}</span>
        ${q.tags ? q.tags.map(function (t) { return `<span class="badge">${esc(t)}</span>`; }).join('') : ''}
      </div>
    `;
  }

  function stemHtml(q) {
    let h = `<div class="stem" style="font-weight:700;line-height:1.8;">${nl2br(q.stem || '')}</div>`;
    const code = getCode(q);

    if (code) {
      h += `<pre class="code">${esc(code)}</pre>`;
    }

    return h;
  }

  function questionHtml(q, prog) {
    let h = questionMetaHtml(q);
    h += stemHtml(q);

    if (q.type === 'single_choice') {
      const options = q.options || {};
      const correctList = Array.isArray(q.answer) ? q.answer : [q.answer];

      Object.keys(options).forEach(function (letter) {
        let cls = 'option';
        const selected = prog.choice === letter;
        if (selected) cls += ' selected';

        if (prog.judged) {
          if (correctList.indexOf(letter) !== -1) cls += ' correct';
          else if (selected) cls += ' wrong';
        }

        h += `
          <button class="${cls}" data-action="option" data-id="${esc(q.id)}" data-letter="${esc(letter)}" ${prog.judged ? 'disabled' : ''}>
            <b>${esc(letter)}.</b> ${esc(options[letter])}
          </button>
        `;
      });
    } else if (q.type === 'fill_blank') {
      const answers = Array.isArray(q.answer) ? q.answer : [q.answer];
      const count = Math.max(1, answers.length, Number(q.blankCount || 1));

      for (let i = 0; i < count; i++) {
        const val = (prog.inputs && prog.inputs[i]) || '';
        h += `
          <div class="fill-item">
            <div class="muted">第 ${i + 1} 空</div>
            <input type="text" class="fill-input" data-index="${i}" value="${esc(val)}" ${prog.judged ? 'disabled' : ''}>
          </div>
        `;
      }

      if (!prog.judged) {
        h += `<button class="btn primary" data-action="submit-fill" data-id="${esc(q.id)}">提交答案</button>`;
      }
    } else if (q.type === 'code_fill') {
      const blanks = q.blanks || [];

      blanks.forEach(function (b, i) {
        const val = (prog.blanks && prog.blanks[i]) || '';
        h += `
          <div class="blank-item">
            <div class="muted">空 ${b.id || (i + 1)}</div>
            <input type="text" class="blank-input" data-index="${i}" value="${esc(val)}" ${prog.judged ? 'disabled' : ''}>
          </div>
        `;
      });

      if (!prog.judged) {
        h += `<button class="btn primary" data-action="submit-codefill" data-id="${esc(q.id)}">提交答案</button>`;
      }
    } else if (q.type === 'code_output') {
      h += `
        <textarea id="output-answer" placeholder="请输入程序运行结果" ${prog.judged ? 'disabled' : ''}>${esc(prog.output || '')}</textarea>
      `;

      if (!prog.judged) {
        h += `<button class="btn primary" data-action="submit-output" data-id="${esc(q.id)}">提交答案</button>`;
      }
    } else if (q.type === 'coding') {
      h += `
        <textarea id="coding-answer" placeholder="请写代码或思路，保存后可查看参考答案并自评" ${prog.judged ? 'disabled' : ''}>${esc(prog.coding || '')}</textarea>
      `;

      if (!prog.saved) {
        h += `<button class="btn primary" data-action="save-coding" data-id="${esc(q.id)}">保存并显示参考答案</button>`;
      } else if (!prog.judged) {
        h += `
          <div class="btn-row">
            <button class="btn" data-action="judge-subj" data-id="${esc(q.id)}" data-correct="1">自评：正确</button>
            <button class="btn danger" data-action="judge-subj" data-id="${esc(q.id)}" data-correct="0">自评：错误</button>
          </div>
        `;
      }
    } else {
      h += `<div class="muted">该题型暂以查看解析为主。</div>`;
    }

    return h;
  }

  function answerBlock(q) {
    if (q.type === 'single_choice') {
      return `<p><b>答案：</b>${esc([].concat(q.answer || []).join('，'))}</p>`;
    }

    if (q.type === 'fill_blank') {
      const ans = [].concat(q.answer || []);
      return `<p><b>答案：</b>${esc(ans.join('；'))}</p>`;
    }

    if (q.type === 'code_fill') {
      const ans = (q.blanks || []).map(function (b, i) {
        return `${b.id || (i + 1)}：${[].concat(b.answers || b.answer || []).join(' / ')}`;
      }).join('；');
      return `<p><b>答案：</b>${esc(ans)}</p>`;
    }

    if (q.type === 'code_output') {
      return `<p><b>参考输出：</b></p><pre class="code">${esc([].concat(q.answer || []).join('\n'))}</pre>`;
    }

    if (q.type === 'coding') {
      const ref = getRef(q);
      return ref ? `<p><b>参考代码：</b></p><pre class="code">${esc(ref)}</pre>` : '';
    }

    return '';
  }

  function analysisHtml(q) {
    let h = '<div class="analysis"><h3>参考答案 / 解析</h3>';
    h += answerBlock(q);

    if (q.analysis) {
      h += `<div style="margin-top:10px;">${nl2br(q.analysis)}</div>`;
    }

    h += '</div>';
    return h;
  }

  function renderQuiz() {
    if (!quiz || !quiz.qids || !quiz.qids.length) {
      location.hash = '#/';
      return;
    }

    const id = quiz.qids[quiz.index];
    const q = qById(id);

    if (!q) {
      view.innerHTML = `
        <div class="card">
          <p>题目不存在</p>
          <button class="btn" data-action="exit-quiz">返回首页</button>
        </div>
      `;
      return;
    }

    const prog = getProgress(id);
    const total = quiz.qids.length;

    let html = `
      <div class="card">
        <div class="quiz-head" style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px;">
          <div class="muted">第 ${quiz.index + 1} / ${total} 题</div>
          <div class="muted">${prog.judged ? (prog.correct ? '已答对' : '已答错') : '未作答'}</div>
        </div>
        ${questionHtml(q, prog)}
        <div class="btn-row">
          <button class="btn" data-action="prev" ${quiz.index === 0 ? 'disabled' : ''}>上一题</button>
          <button class="btn" data-action="next" ${quiz.index === total - 1 ? 'disabled' : ''}>下一题</button>
          <button class="btn primary" data-action="finish">完成</button>
          <button class="btn" data-action="toggle-fav" data-id="${esc(q.id)}">${isFav(q.id) ? '取消收藏' : '收藏'}</button>
          <button class="btn" data-action="toggle-analysis" data-id="${esc(q.id)}">解析</button>
        </div>
      </div>
    `;

    if (prog.judged || prog.showAnalysis) {
      html += analysisHtml(q);
    }

    view.innerHTML = html;
  }

  function checkSingle(q, choice) {
    const ans = Array.isArray(q.answer) ? q.answer : [q.answer];
    return ans.indexOf(choice) !== -1;
  }

  function checkFill(q, inputs) {
    const answers = Array.isArray(q.answer) ? q.answer : [q.answer];
    const count = Math.max(1, answers.length, Number(q.blankCount || 1));

    for (let i = 0; i < count; i++) {
      let expected = [answers[i]];
      if (q.accepted && q.accepted[i]) {
        expected = expected.concat(q.accepted[i]);
      }
      expected = expected.filter(Boolean);

      if (!matchOne(inputs[i] || '', expected)) {
        return false;
      }
    }

    return true;
  }

  function checkCodeFill(q, inputs) {
    return (q.blanks || []).every(function (b, i) {
      const expected = [].concat(b.answers || b.answer || []).filter(Boolean);
      return matchOne(inputs[i] || '', expected);
    });
  }

  function checkOutput(q, output) {
    let expected = [].concat(q.answer || []).filter(Boolean);
    if (q.accepted) {
      expected = expected.concat(q.accepted);
    }
    return matchOutput(output, expected);
  }

  function judgeObjective(q) {
    const prog = getProgress(q.id);
    let correct = false;

    if (q.type === 'single_choice') {
      correct = checkSingle(q, prog.choice);
    }

    setProgress(q.id, { judged: true, correct: correct });

    if (correct) {
      removeWrong(q.id);
    } else {
      addWrong(q.id);
    }

    render();
  }

  function submitFill(id) {
    const inputs = Array.from(view.querySelectorAll('.fill-input')).map(function (x) { return x.value; });
    const q = qById(id);
    const correct = checkFill(q, inputs);

    setProgress(id, { inputs: inputs, judged: true, correct: correct });

    if (correct) removeWrong(id);
    else addWrong(id);

    render();
  }

  function submitCodeFill(id) {
    const inputs = Array.from(view.querySelectorAll('.blank-input')).map(function (x) { return x.value; });
    const q = qById(id);
    const correct = checkCodeFill(q, inputs);

    setProgress(id, { blanks: inputs, judged: true, correct: correct });

    if (correct) removeWrong(id);
    else addWrong(id);

    render();
  }

  function submitOutput(id) {
    const ta = document.getElementById('output-answer');
    const val = ta ? ta.value : '';
    const q = qById(id);
    const correct = checkOutput(q, val);

    setProgress(id, { output: val, judged: true, correct: correct });

    if (correct) removeWrong(id);
    else addWrong(id);

    render();
  }

  function saveCoding(id) {
    const ta = document.getElementById('coding-answer');
    const val = ta ? ta.value : '';

    setProgress(id, {
      coding: val,
      saved: true,
      showAnalysis: true
    });

    render();
  }

  function judgeSubjective(id, correct) {
    setProgress(id, { judged: true, correct: correct });

    if (correct) removeWrong(id);
    else addWrong(id);

    render();
  }

  function renderReport() {
    if (!quiz || !quiz.qids) {
      location.hash = '#/';
      return;
    }

    const items = quiz.qids.map(function (id) {
      return {
        q: qById(id),
        p: getProgress(id)
      };
    }).filter(function (x) { return x.q; });

    const judged = items.filter(function (x) { return x.p.judged; }).length;
    const correct = items.filter(function (x) { return x.p.judged && x.p.correct; }).length;
    const acc = judged ? Math.round(correct / judged * 100) : 0;

    let html = `
      <div class="card">
        <h1>练习结果</h1>
        <div class="stats">
          <div class="stat">总题数<b>${items.length}</b></div>
          <div class="stat">已作答<b>${judged}</b></div>
          <div class="stat">正确<b>${correct}</b></div>
          <div class="stat">正确率<b>${acc}%</b></div>
        </div>
        <div class="btn-row">
          <button class="btn" data-action="exit-quiz">退出本次练习</button>
        </div>
      </div>
      <section class="cards">
    `;

    items.forEach(function (item, idx) {
      const cls = item.p.judged ? (item.p.correct ? 'card good' : 'card bad') : 'card';

      html += `
        <div class="${cls}">
          <div class="q-title">${idx + 1}. ${esc(truncate(item.q.stem || '', 70))}</div>
          <div class="muted" style="margin:8px 0;">${item.p.judged ? (item.p.correct ? '正确' : '错误') : '未作答'}</div>
          <div class="btn-row">
            <button class="btn primary" data-action="start-single" data-id="${esc(item.q.id)}">查看</button>
          </div>
        </div>
      `;
    });

    html += '</section>';
    view.innerHTML = html;
  }

  function renderSettings() {
    view.innerHTML = `
      <div class="card">
        <h1>设置</h1>
        <p class="muted">
          本应用无后端。题库来自静态 JSON 文件；学习进度与自定义题库保存在当前浏览器中。
        </p>
        <div class="btn-row">
          <button class="btn" data-action="export-progress">导出全部本机数据</button>
          <button class="btn" data-action="export-custom">导出自定义题库</button>
          <button class="btn danger" data-action="clear-data">清空本机数据</button>
        </div>
        <div class="btn-row">
          <label class="file-btn">导入本机数据<input type="file" id="importProgress" accept="application/json"></label>
          <label class="file-btn">导入题库JSON<input type="file" id="importQuestions" accept="application/json"></label>
        </div>
      </div>
    `;
  }

  function exportJSON(filename, obj) {
    const blob = new Blob([JSON.stringify(obj, null, 2)], { type: 'application/json' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = filename;
    a.click();
    URL.revokeObjectURL(a.href);
  }

  function readJSONFile(file, cb) {
    if (!file) return;
    const fr = new FileReader();
    fr.onload = function () {
      try {
        cb(JSON.parse(fr.result));
      } catch (e) {
        alert('JSON 解析失败');
      }
    };
    fr.readAsText(file);
  }

  view.addEventListener('click', function (e) {
    const btn = e.target.closest('[data-action]');
    if (!btn) return;

    const action = btn.getAttribute('data-action');
    const id = btn.getAttribute('data-id');

    if (action === 'start') {
      startQuiz(btn.dataset);
    }

    if (action === 'start-single') {
      startSingle(id);
    }

    if (action === 'option') {
      setProgress(id, { choice: btn.getAttribute('data-letter') });
      judgeObjective(qById(id));
    }

    if (action === 'submit-fill') {
      submitFill(id);
    }

    if (action === 'submit-codefill') {
      submitCodeFill(id);
    }

    if (action === 'submit-output') {
      submitOutput(id);
    }

    if (action === 'save-coding') {
      saveCoding(id);
    }

    if (action === 'judge-subj') {
      judgeSubjective(id, btn.getAttribute('data-correct') === '1');
    }

    if (action === 'toggle-fav') {
      toggleFav(id);
    }

    if (action === 'toggle-analysis') {
      const prog = getProgress(id);
      setProgress(id, { showAnalysis: !prog.showAnalysis });
      render();
    }

    if (action === 'prev') {
      quiz.index = Math.max(0, quiz.index - 1);
      saveQuiz();
      render();
    }

    if (action === 'next') {
      quiz.index = Math.min(quiz.qids.length - 1, quiz.index + 1);
      saveQuiz();
      render();
    }

    if (action === 'finish') {
      location.hash = '#/report';
    }

    if (action === 'exit-quiz') {
      quiz = null;
      sessionStorage.removeItem('quiz');
      location.hash = '#/';
    }

    if (action === 'remove-wrong') {
      removeWrong(id);
      render();
    }

    if (action === 'export-progress') {
      exportJSON('cpp-quiz-local-data.json', db);
    }

    if (action === 'export-custom') {
      exportJSON('cpp-quiz-custom-questions.json', { questions: db.customQuestions || [] });
    }

    if (action === 'clear-data') {
      if (confirm('确认清空本机刷题数据？该操作不可恢复。')) {
        localStorage.removeItem(STORAGE_KEY);
        location.reload();
      }
    }
  });

  view.addEventListener('change', function (e) {
    if (e.target.id === 'importProgress') {
      readJSONFile(e.target.files[0], function (data) {
        db = Object.assign({}, defaultDb, data);
        saveDb();
        alert('导入成功');
        render();
      });
    }

    if (e.target.id === 'importQuestions') {
      readJSONFile(e.target.files[0], function (data) {
        let arr = Array.isArray(data) ? data : (data.questions || []);
        const existing = {};

        allQuestions().forEach(function (q) {
          existing[q.id] = true;
        });

        let added = 0;

        arr.forEach(function (q, i) {
          if (!q.id) q.id = 'custom-' + Date.now() + '-' + i;
          if (existing[q.id]) return;

          db.customQuestions.push(q);
          existing[q.id] = true;
          added++;
        });

        saveDb();
        alert('导入成功，新增 ' + added + ' 题');
        render();
      });
    }
  });

  window.addEventListener('hashchange', render);

  async function init() {
    await loadData();
    render();
  }

  init();
})();
```

---

# 六、PWA 文件

## 6.1 manifest.webmanifest

```json
{
  "name": "C++真题刷题",
  "short_name": "C++刷题",
  "start_url": "./",
  "display": "standalone",
  "background_color": "#f3f6fb",
  "theme_color": "#2563eb",
  "icons": []
}
```

> 如果后续想增强安装体验，可以加 `192x192` 和 `512x512` 图标。

---

## 6.2 sw.js

```js
const CACHE_NAME = 'cpp-quiz-v1';
const STATIC_ASSETS = [
  './',
  './index.html',
  './assets/style.css',
  './assets/app.js',
  './manifest.webmanifest'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(STATIC_ASSETS))
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(
        keys
          .filter((key) => key !== CACHE_NAME)
          .map((key) => caches.delete(key))
      )
    )
  );
});

self.addEventListener('fetch', (event) => {
  const url = new URL(event.request.url);

  if (url.pathname.includes('/data/')) {
    event.respondWith(
      fetch(event.request)
        .then((res) => {
          const copy = res.clone();
          caches.open(CACHE_NAME).then((cache) => cache.put(event.request, copy));
          return res;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  event.respondWith(
    caches.match(event.request).then((cached) => {
      return cached || fetch(event.request).then((res) => {
        const copy = res.clone();
        caches.open(CACHE_NAME).then((cache) => cache.put(event.request, copy));
        return res;
      });
    }).catch(() => caches.match('./index.html'))
  );
});
```

---

# 七、题库资源文件示例

下面给你一个可直接运行的示例题库文件。里面包含选择题、填空题、程序阅读题、程序填空题、程序设计题五种题型。

---

## 7.1 data/manifest.json

```json
{
  "files": [
    "data/questions/sample.json"
  ]
}
```

---

## 7.2 data/questions/sample.json

```json
{
  "version": 1,
  "questions": [
    {
      "id": "202604-choice-1",
      "paper": "2026-04",
      "section": "单项选择题",
      "type": "single_choice",
      "stem": "下列C++标识符中，合法的是",
      "options": {
        "A": "123abc",
        "B": "abc_123",
        "C": "int",
        "D": "abc-123"
      },
      "answer": "B",
      "analysis": "C++标识符可以由字母、数字、下划线组成，但不能以数字开头，不能是关键字，也不能包含减号。",
      "tags": ["标识符", "基础"],
      "difficulty": 1,
      "source": "2026年4月真题"
    },
    {
      "id": "202604-fill-15",
      "paper": "2026-04",
      "section": "填空题",
      "type": "fill_blank",
      "stem": "C++中，标准输入流对象是________。",
      "answer": ["cin"],
      "analysis": "C++标准输入流对象是cin，标准输出流对象是cout。",
      "tags": ["输入输出"],
      "difficulty": 1,
      "source": "2026年4月真题"
    },
    {
      "id": "202604-code-output-16",
      "paper": "2026-04",
      "section": "程序阅读题",
      "type": "code_output",
      "stem": "写出下列程序的运行结果。",
      "codeLines": [
        "#include<iostream>",
        "using namespace std;",
        "int main(){",
        "    int a=3, b=5;",
        "    int *p=&a, *q=&b;",
        "    *p=*p+*q;",
        "    cout<< a<<\",\"<< b<< endl;",
        "    return 0;",
        "}"
      ],
      "answer": ["8,5"],
      "accepted": ["8, 5"],
      "analysis": "*p就是变量a，*q就是变量b。执行*p = *p + *q后，a变为8，b仍然为5。",
      "tags": ["指针", "程序阅读"],
      "difficulty": 2,
      "source": "2026年4月真题"
    },
    {
      "id": "202604-code-fill-20",
      "paper": "2026-04",
      "section": "程序填空题",
      "type": "code_fill",
      "stem": "以下程序实现求两个整数的最大值，请补全代码。",
      "codeLines": [
        "#include<iostream>",
        "using namespace std;",
        "int max(____①____){ return a>b? a: b; }",
        "int main(){",
        "    int x,y;",
        "    cin>> x>> y;",
        "    cout<<____②____<< endl;",
        "    return 0;",
        "}"
      ],
      "blanks": [
        {
          "id": "①",
          "answers": ["int a, int b", "int a,int b"]
        },
        {
          "id": "②",
          "answers": ["max(x, y)", "max(x,y)"]
        }
      ],
      "analysis": "函数max需要两个整型形参；主函数中调用max(x,y)得到最大值。",
      "tags": ["函数", "程序填空"],
      "difficulty": 2,
      "source": "2026年4月真题"
    },
    {
      "id": "202604-coding-22",
      "paper": "2026-04",
      "section": "程序设计题",
      "type": "coding",
      "stem": "编写一个程序，定义一个学生类Student，包含学号、姓名、成绩，并能输出学生信息。",
      "referenceLines": [
        "#include <iostream>",
        "#include <string>",
        "using namespace std;",
        "",
        "class Student {",
        "private:",
        "    string id;",
        "    string name;",
        "    double score;",
        "public:",
        "    Student(string i, string n, double s) : id(i), name(n), score(s) {}",
        "    void showInfo() {",
        "        cout << id << ' ' << name << ' ' << score << endl;",
        "    }",
        "};",
        "",
        "int main() {",
        "    Student s(\"2026001\", \"ZhangSan\", 90.5);",
        "    s.showInfo();",
        "    return 0;",
        "}"
      ],
      "analysis": "类中通常将数据成员设为私有，通过公有构造函数初始化数据成员，通过公有成员函数完成输出。",
      "tags": ["类与对象", "程序设计"],
      "difficulty": 3,
      "source": "2026年4月真题"
    },
    {
      "id": "202404-choice-1",
      "paper": "2024-04",
      "section": "单项选择题",
      "type": "single_choice",
      "stem": "要建立良好的程序设计风格，描述正确的是",
      "options": {
        "A": "无需给程序添加注释",
        "B": "变量命名只要符合语法",
        "C": "首先考虑程序的执行效率",
        "D": "程序应清晰、可读性好"
      },
      "answer": "D",
      "analysis": "良好的程序设计风格强调程序清晰、可读性好，注释、命名规范、结构清楚都很重要。",
      "tags": ["程序设计风格"],
      "difficulty": 1,
      "source": "2024年4月真题"
    },
    {
      "id": "202404-fill-22",
      "paper": "2024-04",
      "section": "填空题",
      "type": "fill_blank",
      "stem": "执行代码 string str(\"NihaoC++\"); cout<<str.substr(5,3); 的输出结果是________。",
      "answer": ["C++"],
      "analysis": "字符串下标从0开始，下标5是C，从下标5开始取3个字符为C++。",
      "tags": ["string", "substr"],
      "difficulty": 2,
      "source": "2024年4月真题"
    }
  ]
}
```

---

# 八、题库管理脚本

为了支持新增题目，建议加两个小脚本。

---

## 8.1 tools/build-manifest.mjs

作用：自动扫描 `data/questions/` 目录下所有 JSON，并生成 `data/manifest.json`。

```js
import { readdirSync, writeFileSync } from 'fs';

const dir = 'data/questions';

const files = readdirSync(dir)
  .filter(file => file.endsWith('.json'))
  .sort()
  .map(file => `data/questions/${file}`);

const manifest = { files };

writeFileSync('data/manifest.json', JSON.stringify(manifest, null, 2));

console.log('manifest.json 已更新，共加载题库文件：', files.length);
```

运行：

```bash
node tools/build-manifest.mjs
```

---

## 8.2 tools/validate.mjs

作用：简单校验题库 JSON。

```js
import { readFileSync } from 'fs';

const file = process.argv[2];

if (!file) {
  console.error('用法: node tools/validate.mjs data/questions/sample.json');
  process.exit(1);
}

const raw = readFileSync(file, 'utf8');
const data = JSON.parse(raw);

const questions = Array.isArray(data) ? data : data.questions;

if (!Array.isArray(questions)) {
  console.error('错误：JSON 中必须包含 questions 数组');
  process.exit(1);
}

const ids = new Set();

questions.forEach((q, index) => {
  if (!q.id) {
    console.error(`错误：第 ${index + 1} 题缺少 id`);
    process.exit(1);
  }

  if (ids.has(q.id)) {
    console.error(`错误：题目 id 重复：${q.id}`);
    process.exit(1);
  }

  ids.add(q.id);

  if (!q.type) {
    console.error(`错误：题目 ${q.id} 缺少 type`);
    process.exit(1);
  }

  if (!q.stem && !q.code && !q.codeLines) {
    console.error(`警告：题目 ${q.id} 可能缺少题干`);
  }

  if (q.type === 'single_choice') {
    if (!q.options) {
      console.error(`错误：选择题 ${q.id} 缺少 options`);
      process.exit(1);
    }

    if (!q.answer) {
      console.error(`错误：选择题 ${q.id} 缺少 answer`);
      process.exit(1);
    }
  }
});

console.log('校验通过：', file);
```

运行：

```bash
node tools/validate.mjs data/questions/sample.json
```

---

# 九、新增题目的三种方式

## 方式一：直接修改 JSON 文件

适合正式题库更新。

步骤：

1. 打开 `data/questions/2026-04.json`
2. 在 `questions` 数组末尾追加题目
3. 保证 `id` 唯一
4. 运行校验：

```bash
node tools/validate.mjs data/questions/2026-04.json
```

5. 提交到 GitHub

---

## 方式二：新增 JSON 文件

适合新增一套卷子。

步骤：

1. 新建 `data/questions/2026-10.json`
2. 写入：

```json
{
  "version": 1,
  "questions": []
}
```

3. 运行：

```bash
node tools/build-manifest.mjs
```

4. 提交到 GitHub

---

## 方式三：网页内本地导入

适合你临时增加题目，不改仓库。

步骤：

1. 准备一个 JSON 文件：

```json
{
  "questions": [
    {
      "id": "custom-choice-1",
      "paper": "自定义",
      "section": "单项选择题",
      "type": "single_choice",
      "stem": "C++中哪个关键字用于定义类？",
      "options": {
        "A": "class",
        "B": "struct",
        "C": "object",
        "D": "new"
      },
      "answer": "A",
      "analysis": "class用于定义类。struct也可以定义类，但本题问通常关键字。"
    }
  ]
}
```

2. 打开网站设置页
3. 点击“导入题库JSON”
4. 导入后会保存在当前浏览器中

---

# 十、AI 参考答案生成流程

你要求“AI 参考答题直接根据题目生成”，这里建议采用**预生成模式**，不要让网页运行时调用 AI 接口。

原因：

- GitHub Pages 无后端
- 不依赖 API Key
- 不需要联网 AI
- 题库数据可版本管理
- 手机、PC 都稳定

---

## 10.1 AI 生成字段

每道题由 AI 生成以下内容：

| 字段             | 生成内容       |
| ---------------- | -------------- |
| `stem`           | 清洗后的题干   |
| `options`        | 选择题选项     |
| `answer`         | 标准答案       |
| `accepted`       | 等价答案       |
| `codeLines`      | 程序代码逐行   |
| `blanks`         | 程序填空答案   |
| `referenceLines` | 编程题参考代码 |
| `analysis`       | 中文解析       |
| `tags`           | 知识点标签     |
| `difficulty`     | 难度           |

---

## 10.2 批量解析提示词

你后续把每一套真题文本交给 AI 时，可以直接用下面这个提示词。

```text
你是C++真题解析助手。请将我给出的自考C++试卷文本转换为题库JSON。

要求：
1. 严格输出JSON，不要输出Markdown，不要解释。
2. 每道题必须包含 id、paper、section、type、stem、answer、analysis。
3. 程序代码必须使用 codeLines 数组，逐行保存。
4. 编程题参考代码必须使用 referenceLines 数组。
5. 程序填空题必须使用 code_fill 类型，并用 blanks 数组表示每个空。
6. 程序阅读题必须使用 code_output 类型，answer 为程序输出。
7. 填空题使用 fill_blank 类型，多个空用 answer 数组表示。
8. 选择题使用 single_choice 类型，options 为对象。
9. 答案必须经过C++规则推理，不能瞎猜。
10. 如果一道题存在多个等价答案，请尽量写入 accepted。
11. analysis 用中文解释，适合自考生理解。
12. id 格式：年份月份-题型-题号，例如 202404-choice-1。
13. paper 格式：YYYY-MM，例如 2024-04。
14. 输出结构必须为：
{
  "version": 1,
  "questions": []
}

请开始转换以下试卷内容：
```

---

## 10.3 分批生成建议

为了避免内容截断，建议按如下批次生成：

### 第一批：选择题

```text
请只转换本卷全部单项选择题。
```

### 第二批：填空题

```text
请只转换本卷全部填空题。
```

### 第三批：程序填空题

```text
请只转换本卷全部程序填空题。
```

### 第四批：程序分析题

```text
请只转换本卷全部程序分析题。
```

### 第五批：程序设计题

```text
请只转换本卷全部程序设计题。
```

每批生成后保存为一个 JSON 文件，最后合并到对应年份文件中。

---

# 十一、GitHub Pages 部署步骤

## 11.1 创建仓库

```bash
mkdir cpp-quiz
cd cpp-quiz
git init
```

把上面的文件按目录创建好。

---

## 11.2 本地测试

不要直接双击 `index.html`，因为浏览器会限制 `fetch` 本地 JSON。

推荐：

```bash
npx serve
```

或者：

```bash
python -m http.server 8000
```

然后访问：

```text
http://localhost:8000
```

---

## 11.3 推送到 GitHub

```bash
git add .
git commit -m "init cpp quiz"
git branch -M main
git remote add origin 你的仓库地址
git push -u origin main
```

---

## 11.4 开启 GitHub Pages

1. 打开 GitHub 仓库
2. Settings
3. Pages
4. Source 选择 Deploy from a branch
5. Branch 选择 `main`
6. 目录选择 `/ (root)`
7. Save

稍后访问：

```text
https://你的用户名.github.io/你的仓库名/
```

---

# 十二、PC / 手机体验优化

当前代码已经做了移动端适配：

- 底部 Tab 导航
- 卡片式布局
- 按钮足够大
- 代码块可横向滚动
- 输入框适配移动端
- PWA 可加到桌面

后续可继续增强：

| 功能          | 说明                         |
| ------------- | ---------------------------- |
| 搜索题目      | 按题干、标签、年份搜索       |
| 知识点分类    | 按类、指针、继承、多态等分类 |
| 每日练习      | 随机抽取 10 题               |
| 模拟考试      | 按年份限时                   |
| 错题重做      | 只做错题                     |
| 背题模式      | 直接显示答案和解析           |
| 代码高亮      | 接入 highlight.js            |
| Markdown 解析 | 支持富文本题干               |
| 云同步        | 通过 WebDAV/Gist 手动同步    |

---

# 十三、后续完整真题题库生产步骤

接下来最合理的工作流是：

## Step 1：部署当前网站框架

先保证网站能运行。

---

## Step 2：生成 2024-04 完整题库

按 JSON 格式生成：

```text
data/questions/2024-04.json
```

包含：

- 20 道选择题
- 15 道填空题
- 5 道程序填空题
- 5 道程序分析题
- 2 道程序设计题

---

## Step 3：生成 2024-10 完整题库

生成：

```text
data/questions/2024-10.json
```

---

## Step 4：生成 2025-04 完整题库

生成：

```text
data/questions/2025-04.json
```

---

## Step 5：生成 2025-10 完整题库

生成：

```text
data/questions/2025-10.json
```

---

## Step 6：生成 2026-04 完整题库

生成：

```text
data/questions/2026-04.json
```

---

## Step 7：更新 manifest

```bash
node tools/build-manifest.mjs
```

---

## Step 8：部署到 GitHub Pages

提交并推送即可。

---

# 十四、你现在可以直接使用的最小版本

当前方案已经满足：

- PC / 手机访问
- 无后端
- GitHub Pages 部署
- JSON 题库资源文件
- 新增题目
- 自动判分
- 错题本
- 收藏夹
- 本地进度
- AI 参考答案字段
- PWA 基础支持

---

# 十五、下一步建议

如果你希望我继续“直接把所有真题解析完”，建议按下面顺序分批进行：

1. **先输出 2024年4月完整题库 JSON**
2. **再输出 2024年10月完整题库 JSON**
3. **再输出 2025年4月完整题库 JSON**
4. **再输出 2025年10月完整题库 JSON**
5. **最后输出 2026年4月完整题库 JSON**

这样不会一次输出过长导致截断，并且每一批你都可以直接保存到对应的 `data/questions/` 文件中。